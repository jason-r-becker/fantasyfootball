# No environment variables are currently required.
# Sleeper uses public read-only endpoints. Private ESPN authentication belongs
# only in the ignored per-league config.json described in docs/configuration.md.
